# tabletop-kernel

Transport-agnostic runtime kernel for tabletop and board-game rules engines.

This repo is organized as a Bun workspace. The first implementation target is the
publishable kernel package in `packages/tabletop-kernel`.

Common commands:

```bash
bun install
bun run typecheck
bun run test
```
