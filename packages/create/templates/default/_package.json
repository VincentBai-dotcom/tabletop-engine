{
  "name": "{{projectName}}",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tvk dev",
    "build": "pnpm -C client build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@tableverse-kit/cli": "{{tableverseVersion}}",
    "@tableverse-kit/config": "{{tableverseVersion}}",
    "typescript": "^5"
  }
}
